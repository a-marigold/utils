{
    "name": "utils",
    "version": "1.0.0",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "build": "bun build ./src/index.ts --minify --bytecode --compile --outfile=./dist/utils"
    },
    "bin": {
        "utils": "./dist/utils.exe"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    }
}
