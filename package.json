{
    "name": "utils",
    "version": "1.0.0",
    "type": "module",
    "private": false,
    "scripts": {
        "build": "bun build ./src/index.ts --minify --bytecode --compile --outfile=./dist/utils"
    },
    "bin": {
        "utils": "./dist/utils.exe"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    }
}
